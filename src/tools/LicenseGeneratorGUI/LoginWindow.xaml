<Window x:Class="LicenseGeneratorGUI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Logowanie" Height="250" Width="400" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    
    <Border Background="#1E2A38" CornerRadius="8" BorderBrush="#3F4E5D" BorderThickness="1">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="DAPNET GENERATOR" FontSize="18" FontWeight="Bold" Foreground="#3F72AF" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <TextBlock Grid.Row="1" Text="PODAJ HASŁO DOSTĘPU:" FontSize="10" Foreground="#9CA3AF" Margin="0,0,0,5"/>
            <PasswordBox Grid.Row="2" x:Name="TxtPassword" Height="35" Background="#283645" Foreground="White" BorderBrush="#3F4E5D" VerticalContentAlignment="Center" Padding="10,0"/>
            
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <!-- PRZYCISK ZALOGUJ -->
                <Button Content="ZALOGUJ" Width="120" Height="35" Background="#3F72AF" Foreground="#D0D0D0" Click="BtnLogin_Click" IsDefault="True">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#4F82BF"/>
                                    <Setter Property="RenderTransform">
                                        <Setter.Value><ScaleTransform ScaleX="1.05" ScaleY="1.05"/></Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.RenderTransformOrigin>0.5,0.5</Button.RenderTransformOrigin>
                </Button>

                <!-- PRZYCISK WYJDŹ (POPRAWIONY KOLOR TEKSTU) -->
                <Button Width="100" Height="35" Click="BtnExit_Click" Margin="10,0,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="Transparent" CornerRadius="4" BorderBrush="#A93226" BorderThickness="1">
                                <TextBlock x:Name="txt" Text="WYJDŹ" Foreground="#A93226" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#A93226"/>
                                    <Setter TargetName="txt" Property="Foreground" Value="White"/>
                                    <Setter Property="RenderTransform">
                                        <Setter.Value><ScaleTransform ScaleX="1.05" ScaleY="1.05"/></Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.RenderTransformOrigin>0.5,0.5</Button.RenderTransformOrigin>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
